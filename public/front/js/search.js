
$(function(){

/**在进行搜索历史记录管理时,需要维护一个数组,这个数组需要存在本地存储中
 * 我们约定一个键名(key),search_list
 *
 */



 //1. 搜索离职记录渲染功能
/**(1) 获取本地存储中存储的数据 JSONStr
 * (2) 转换成数组
 * (3) 将数组,通过模板引擎进行渲染
 *    遍历的是数组,每一项是v
 *
 */

    var history = localStorage.getItem( '' );
    var arr = JSON.parse()












})




















